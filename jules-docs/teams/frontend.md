# Frontend Team Constraints
- React (Vite) Single Page Application.
- TailwindCSS styling.
- Ensure all Fetch/Axios URLs utilize `VITE_API_BASE_URL`.
- Strip out mock data fallbacks; prefer empty states.
